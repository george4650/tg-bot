package Logic

/*func Create(p Model.Product) error {
	err := uc.s.AddToCart(p)
	if err != nil {
		return err
	}
	return nil
}

func GetAllProducts() ([]Model.Product, error) {
	p, err := Repository.GetAllProducts()
	if err != nil {
		return nil, err
	}
	return p, nil
}

func ReadOne(id string) (*Model.Product, error) {
	product_id, err := strconv.Atoi(id)
	if err != nil {
		return nil, fmt.Errorf("неверно введён параметр id: %v", err)
	}
	product, err := uc.s.ReadOne(product_id)
	if err != nil {
		return nil, err
	}
	p := Model.Product{}
	if p == *product {
		return nil, sql.ErrNoRows
	}
	return product, nil
}
*/
